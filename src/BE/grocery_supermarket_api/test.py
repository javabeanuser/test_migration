from model import Item

# engine = create_engine(Config.get_database_url())
#
# # create session and add objects
# with Session(engine) as session:
#
#     session.add(User("Admin", 1, "password11", [Order(1, 4), Order(1, 5)]))
#     session.commit()
